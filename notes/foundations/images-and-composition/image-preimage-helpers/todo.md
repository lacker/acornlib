# Image/Preimage Helpers

Goal: make the standard `f(s)` and `f⁻¹(t)` identities available as reusable lemmas instead of reproving witness arguments locally.

Completed. Continue from [the parent notes](../todo.md).
