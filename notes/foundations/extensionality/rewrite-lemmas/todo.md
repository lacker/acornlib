# Membership Rewrite Lemmas

Goal: make routine membership calculations reduce to pointwise Boolean equalities instead of ad hoc case splits.

- [ ] Add an insert membership rewrite lemma
- [ ] Add a remove membership rewrite lemma
- [ ] Audit existing set proofs and replace local membership expansions with the new rewrite lemmas
