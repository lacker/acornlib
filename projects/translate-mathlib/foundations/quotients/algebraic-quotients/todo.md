# Algebraic Quotients

Goal: build group, ring, and module quotient APIs on top of the foundational `Quotient` / `QuotientOver` family, starting from kernel-of-homomorphism congruences.

Status:

- Decision (resolved): the per-`qrel` kernel-quotient operations stay as the load-bearing layer; do **not** add a generic typeclass instance on `Quotient[A]` / `QuotientOver[A]`. When a concrete quotient needs typeclass-level structure (so consumers can write `+` and reuse generic algebraic theorems), wrap it in a fresh struct parameterised by the type-level data that determines the qrel and put the typeclass instance on the wrapper. `Zmod[n]` already follows this shape and is the reference example. Why: the qrel is value-level, so a single instance on `QuotientOver[T]` cannot dispatch to the right group/ring structure for different qrels of the same `T`; per-qrel functions are uniformly available, but generic instances would have to commit to one quotient. How to apply: prefer wrapping per-quotient when typeclass ergonomics are needed; otherwise stay on the existing `<scope>_quotient_*` API.
- Future option (not implemented): a phantom singleton encoding `QuotientOver[T, Q]` where `Q` is a zero-element type that recovers the qrel via its own typeclass instance would give one type family with automatic dispatch. Pursue only when a real consumer needs to be generic over an arbitrary qrel — at that point, the per-quotient wrapper boilerplate becomes the deciding pain.



- `add_monoid_hom_kernel_respects_add` and `add_monoid_hom_kernel_is_add_congruence` in `src/quotient_algebra.ac` give the additive monoid bridge.
- `add_group_hom_kernel_respects_add`, `add_group_hom_kernel_respects_neg`, and `add_group_hom_kernel_is_add_congruence` in `src/quotient_algebra.ac` give the additive group bridge.
- `monoid_hom_kernel_respects_mul` and `monoid_hom_kernel_is_mul_congruence` give the multiplicative monoid bridge.
- `group_hom_kernel_respects_mul`, `group_hom_kernel_respects_inverse`, and `group_hom_kernel_is_mul_congruence` give the multiplicative group bridge.
- `ring_hom_kernel_respects_add`, `ring_hom_kernel_respects_mul`, `ring_hom_kernel_is_add_congruence`, `ring_hom_kernel_is_mul_congruence`, and `ring_hom_kernel_is_ring_congruence` give the ring bridge, taking bundled `RingHom[R, S]` directly. `is_ring_hom` is defined in `src/ring_hom.ac`; `is_ring_congruence` is defined in `src/quotient_algebra.ac`.
- Per-`qrel` kernel-quotient operation lifts in `src/quotient_algebra.ac`: `add_group_hom_kernel_quotient_relation_respects_add` / `_respects_neg`, the lifted operations `add_group_hom_kernel_quotient_add` / `_neg`, and projection lemmas (`_projection`, `_projection_left`, `_projection_right`, `_projection_compatible`) for `AddGroupHom`. Parallel multiplicative versions exist for `GroupHom`: `group_hom_kernel_quotient_relation_respects_mul` / `_respects_inverse`, `group_hom_kernel_quotient_mul` / `_inverse`, and matching projection lemmas. Full additive-group axioms are also in place for `AddGroupHom` (`_zero` definition plus `_add_assoc`, `_zero_add`, `_add_zero`, `_neg_add`, `_add_neg`) and full group axioms for `GroupHom` (`_one` definition plus `_mul_assoc`, `_one_mul`, `_mul_one`, `_inverse_left`, `_inverse_right`).
- For `RingHom[R, S]`, `src/quotient_algebra.ac` has `ring_hom_kernel_quotient_relation_respects_add` / `_respects_mul` / `_respects_neg`, the lifted operations `ring_hom_kernel_quotient_add` / `_mul` / `_neg`, and the same projection lemmas (equal-representative and related-representative cases) for each. Full ring axioms are also in place: `ring_hom_kernel_quotient_zero` / `_one` definitions, additive-group axioms (`_add_assoc`, `_zero_add`, `_add_zero`, `_neg_add`, `_add_neg`, `_add_comm`), multiplicative monoid axioms (`_mul_assoc`, `_one_mul`, `_mul_one`), distributivity (`_distrib_left`, `_distrib_right`), and mul-zero (`_mul_zero_left`, `_mul_zero_right`), all proved at the representative level via the substitute-through-projection chain pattern.
- For `MonoidHom[M, N]` and `AddMonoidHom[A, B]`, `src/quotient_algebra.ac` has the corresponding kernel-quotient lifts: `monoid_hom_kernel_quotient_relation_respects_mul` / `monoid_hom_kernel_quotient_mul` plus projections, and `add_monoid_hom_kernel_quotient_relation_respects_add` / `add_monoid_hom_kernel_quotient_add` plus projections. Full monoid axioms are also in place: `monoid_hom_kernel_quotient_one` / `_mul_assoc` / `_one_mul` / `_mul_one`, and `add_monoid_hom_kernel_quotient_zero` / `_add_assoc` / `_zero_add` / `_add_zero`.
- For predicate-form linear maps (`is_linear_map(src, dst, f)`), `src/quotient_algebra.ac` has `linear_map_kernel_quotient_relation_respects_add`, `linear_map_kernel_respects_smul` (per-`r`), `linear_map_kernel_quotient_relation_respects_smul`, the lifted operations `linear_map_kernel_quotient_add` and per-scalar `linear_map_kernel_quotient_smul(src, dst, f, r)`, and the matching projection lemmas. Additive-commutative-group axioms are also in place: `linear_map_kernel_quotient_zero` definition plus `_add_assoc`, `_zero_add`, `_add_zero`, `_add_comm`, all under the `is_linear_map(src, dst, f)` premise; `_assoc` and `_comm` needed an explicit final transitivity step at the end of the chain to fit within the search budget. Module-action axioms `_smul_one`, `_smul_assoc`, `_smul_add_right` (smul over quotient addition), `_smul_add_left` (sum of scalars), `_smul_zero_left` (zero scalar), and `_smul_zero_right` (zero element) are likewise in place under the same premise.
- Commutativity axioms are in place for the matching commutative cases: `ring_hom_kernel_quotient_mul_comm` for `R: CommRing` and `group_hom_kernel_quotient_mul_comm` for `G: CommGroup`.
- Negation is now lifted for the linear-map kernel quotient: `linear_map_kernel_respects_neg`, `linear_map_kernel_quotient_relation_respects_neg`, the lifted operation `linear_map_kernel_quotient_neg` (defined via `quotient_over_unary_op`), and the projection lemmas `_projection` / `_projection_compatible`. Inverse axioms `_neg_add` and `_add_neg` complete the additive-commutative-group structure on the module quotient.
- `linear_map_kernel_respects_add`, `linear_map_kernel_smul_step`, and `linear_map_kernel_is_add_congruence` give the module-hom (R-linear map) bridge in predicate form (using `is_linear_map(src, dst, f)`), capturing both the additive congruence and the per-scalar smul preservation that makes the kernel a submodule.
- Kernel quotient operations now lift through `quotient_over_binary_op`, `quotient_over_unary_op`, and `quotient_over_const` for additive monoid addition/zero, additive group addition/zero/negation/subtraction, monoid multiplication/one, group multiplication/one/inversion, ring addition/zero/negation/subtraction/multiplication/one, and predicate-form linear-map addition/zero/scalar multiplication. Representative projection lemmas and projected identity laws are proved for the bundled algebraic homomorphism cases; the predicate-form linear-map addition projection and projected zero laws verify under an `is_linear_map` premise, while stronger implication-heavy scalar projection statements remain deferred.
- All kernel bridges take struct-form homomorphisms (`AddGroupHom`, `MonoidHom`, `GroupHom`) so the existing `_add` / `_mul` / `_neg` / `_inv` / `_zero` / `_one` lemmas apply directly. A predicate-level form was attempted first but the identity-derivation proofs timed out under default search; the struct path is the durable choice.
- `src/add_subgroup.ac` defines `AddSubgroup[G: AddGroup]` (via `add_zero_constraint`, `add_closure_constraint`, `neg_constraint`, `add_subgroup_constraint`), the closure lemmas `add_subgroup_contains_zero` / `_add` / `_neg` / `_sub`, the relation `add_subgroup_rel(h, a, b) := h.contains(a - b)`, equivalence proofs (`_is_reflexive` / `_is_symmetric` / `_is_transitive` / `_is_equivalence`), and the constraint-satisfying `add_subgroup_quotient_relation` together with `add_subgroup_quotient_relation_rel`. The transitivity proof needed an explicit `is_transitive(...) = forall(...)` unfolding step to fit within the search budget.
- For `AddCommGroup`, `add_subgroup_rel_add_step`, `add_subgroup_rel_respects_add`, and `add_subgroup_rel_is_add_congruence` in `src/add_subgroup.ac` show the subgroup quotient relation is an additive congruence; the helper `add_sub_distrib` packages the regrouping `(a + b) - (a' + b') = (a - a') + (b - b')`.
- Negation is a quotient unary operation on the subgroup quotient relation: `add_neg_sub`, `add_subgroup_rel_neg_step`, `add_subgroup_rel_respects_neg`, `add_subgroup_quotient_relation_respects_neg`, and `add_subgroup_quotient_relation_respects_add` (the latter rephrasing the additive congruence as `quotient_binary_respects` so it plugs straight into `quotient_over_binary_op_projection`).
- The lifted operations `add_subgroup_quotient_add` and `add_subgroup_quotient_neg` on `QuotientOver[G]` are in `src/add_subgroup.ac`, with projection-compatibility lemmas `add_subgroup_quotient_add_projection`, `_left`, `_right`, `add_subgroup_quotient_neg_projection`, and `_compatible`.
- Full additive-commutative-group axioms for the additive-subgroup quotient are in `src/add_subgroup.ac`: `add_subgroup_quotient_zero(h) := quotient_over_mk(qrel, G.0)`, plus `add_subgroup_quotient_add_assoc`, `_zero_add`, `_add_zero`, `_neg_add`, `_add_neg`, and `_add_comm`. The `AddCommGroup` case verified on first try with the same explicit substitute-through-projection chain pattern (no `is_normal` premise to discharge).
- `src/normal_subgroup.ac` defines `is_normal_subgroup`, `normal_conjugate_mem`, `normal_subgroup_rel(s, a, b) := s.contains(a * b.inverse)`, the pointwise equivalence helpers `normal_subgroup_rel_reflexive` / `_symmetric` / `_trans_at`, and the forall-form `normal_subgroup_rel_is_reflexive` / `_is_symmetric` / `_is_transitive` / `_is_equivalence`. The `is_transitive` named wrapper now goes through by aliasing the relation to a local `let r` and stating both directions of the unfolding equality explicitly — the typeclass resolution cost that timed out previously is hidden by the alias.
- `src/normal_subgroup.ac` also has the pointwise multiplicative and inverse step lemmas: `normal_mul_inverse_distrib` regroups `(a * b) * (ap * bp).inverse` as `(a * (b * bp.inverse) * a.inverse) * (a * ap.inverse)`; `normal_subgroup_rel_mul_step` and `normal_subgroup_rel_inv_step` show that `normal_subgroup_rel(s)` is preserved by pairwise multiplication and by inversion when `is_normal_subgroup(s)` holds. The forall-form `normal_subgroup_rel_respects_mul` and `normal_subgroup_rel_respects_inverse` (`is_normal_subgroup(s)` premise) follow.
- `src/normal_subgroup.ac` packages the equivalence as `normal_subgroup_quotient_relation(s) : QuotientRelation[G]` with round-trip lemma `normal_subgroup_quotient_relation_rel`, the multiplicative congruence proof `normal_subgroup_rel_is_mul_congruence` (under `is_normal_subgroup(s)`), and the `quotient_binary_respects` / `quotient_unary_respects` versions for `G.mul` / `G.inverse`. Lifted operations `normal_subgroup_quotient_mul` and `normal_subgroup_quotient_inverse` on `QuotientOver[G]` are defined via `quotient_over_binary_op` / `quotient_over_unary_op`, with projection lemmas (`_projection`, `_projection_left`, `_projection_right`, `_projection_compatible`) all guarded by the `is_normal_subgroup(s)` premise.
- The full quotient-group axioms are in `src/normal_subgroup.ac`: `normal_subgroup_quotient_one(s) := quotient_over_mk(qrel, G.1)`, plus `normal_subgroup_quotient_mul_assoc`, `_one_mul`, `_mul_one`, `_inverse_left`, and `_inverse_right`, all stated for `quotient_over_mk` representatives. Each axiom proof had to spell out the full equality chain (substitute through projection, simplify in `G`, conclude) explicitly because the search budget was insufficient to discover the chain on its own under the multiplicative-typeclass cost.
